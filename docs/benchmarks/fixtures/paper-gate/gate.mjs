// Фикстура для корпуса бенчмарка: гейт, который ЗАЯВЛЯЕТ блокировку и не умеет блокировать.
// Печатает отказ и выходит с нулём — ровно форма из реестра (2026-06-07, 2026-06-10).
// НЕ рабочий код движка: он существует, чтобы у DR8 было на чём доказать, что она ловит.
console.log('Commit refused: violation found');
process.exit(0);
