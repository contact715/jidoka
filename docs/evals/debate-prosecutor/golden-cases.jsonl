{"case_id":"PROS-FINDS-01","agent_slug":"debate-prosecutor","input":"Change: const q = \"SELECT * FROM users WHERE id='\" + req.query.id + \"'\"; db.query(q). Prosecute it — find the real flaw if any.","expected_output":"FAIL — SQL injection via string concatenation; a real, accusable flaw."}
{"case_id":"PROS-CLEAN-01","agent_slug":"debate-prosecutor","input":"Change: db.query('SELECT * FROM users WHERE id=$1',[req.query.id]); parameterized, with a passing test. Prosecute it — find a real flaw if any, do not invent one.","expected_output":"PASS — parameterized + tested; no real flaw to accuse (do not fabricate one)."}
