## Clarifications

_Feature: spec-reach-and-memory-evidence · обновлено: 2026-08-08T19:06:12.711Z_

### ✓ Проблема / job-to-be-done  (`problem` — clear)
Спеку читали, когда вспоминали, потому что контекст был только по запросу. Память проверялась собственным самотестом, то есть кодом, который сам же писал ожидание.

### ✓ Пользователи и роли  (`users` — clear)
Сессия, правящая файл движка; тот, кто судит, работает ли поиск по памяти.

### ✓ Бизнес-метрика и измерение  (`value-metric` — clear)
Доля файлов движка под спекой: было 46 из 247, стало 61. Внешних случаев для памяти было ноль, стало восемь по пяти способностям.

### ✓ Объём: что входит и что НЕ входит  (`scope` — clear)
Входит: индекс путей по упоминаниям, вставка при правке, обратная ось как отчёт, золотые случаи памяти. НЕ входит: блокировка правки файла без спеки и пробуждение векторного слоя.

### ⏸ Ограничения  (`constraints` — deferred)
> Отложено: спека написана после кода (класс spec-written-after-the-code); вопрос относится к стадии ДО постройки и задним числом ответ на него был бы реконструкцией, а не свидетельством

### ⏸ Данные и сущности  (`data` — deferred)
> Отложено: спека написана после кода (класс spec-written-after-the-code); вопрос относится к стадии ДО постройки и задним числом ответ на него был бы реконструкцией, а не свидетельством

### ✓ Краевые случаи и ошибки  (`edge-cases` — clear)
Одиночное упоминание пути, каталог с совпадающим префиксом, файл вне известных корней, случай без проверяемого ожидания, результат без поля релевантности.

### ✓ Критерии приёмки (DoD)  (`success-criteria` — clear)
Правка файла под спекой добавляет строку с её именем, файл вне индекса не даёт ничего, ответ на вопрос без ответа в корпусе засчитывается как ПРОВАЛ воздержания.

### ⏸ Риски и зависимости  (`risks-deps` — deferred)
> Отложено: спека написана после кода (класс spec-written-after-the-code); вопрос относится к стадии ДО постройки и задним числом ответ на него был бы реконструкцией, а не свидетельством
