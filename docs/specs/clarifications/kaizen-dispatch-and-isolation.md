## Clarifications

_Feature: kaizen-dispatch-and-isolation · обновлено: 2026-08-08T19:06:12.287Z_

### ✓ Проблема / job-to-be-done  (`problem` — clear)
Приёмка плана падала до нуля: план жил в отчёте, одобрение в переписке, очередь автономных сессий стояла пустая. Отдельно: изолированная копия была непригодна для запуска, поэтому сессии правили общее дерево, и 66 чужих файлов уехали в чужой коммит.

### ✓ Пользователи и роли  (`users` — clear)
Владелец как источник одобрения; автономная сессия как исполнитель, берущий по одной задаче.

### ✓ Бизнес-метрика и измерение  (`value-metric` — clear)
Приёмка по когортам: была 0% и 6% на двух последних неделях, стала 100% на обеих после внедрения моста. Семнадцать пунктов прошли из отчёта в очередь и закрыты по одному.

### ✓ Объём: что входит и что НЕ входит  (`scope` — clear)
Входит: переходник между тремя существующими реестрами и жизненный цикл изолированной копии. НЕ входит: автоматический выбор того, что внедрять, и параллельный прогон очереди.

### ⏸ Ограничения  (`constraints` — deferred)
> Отложено: спека написана после кода (класс spec-written-after-the-code); вопрос относится к стадии ДО постройки и задним числом ответ на него был бы реконструкцией, а не свидетельством

### ⏸ Данные и сущности  (`data` — deferred)
> Отложено: спека написана после кода (класс spec-written-after-the-code); вопрос относится к стадии ДО постройки и задним числом ответ на него был бы реконструкцией, а не свидетельством

### ✓ Краевые случаи и ошибки  (`edge-cases` — clear)
Отсутствующий путь засева, каталог, объявленный копией, незакоммиченные правки при сносе, неотправленный коммит при сносе, отгруженная рекомендация в отборе.

### ✓ Критерии приёмки (DoD)  (`success-criteria` — clear)
Без явного отбора не ставится ничего, задание самодостаточно и требует доказательства, снос изолированной копии отказывается терять работу.

### ⏸ Риски и зависимости  (`risks-deps` — deferred)
> Отложено: спека написана после кода (класс spec-written-after-the-code); вопрос относится к стадии ДО постройки и задним числом ответ на него был бы реконструкцией, а не свидетельством
