# Runtime state — not source
.sdd-halt-state.json
.sdd-precommit-sentinel
*-queue/
memory-staging/
worktrees/

# Generated telemetry / verdict streams — regenerated by validators, not source.
# (meta-mistakes.jsonl is the engine's INPUT ledger and stays versioned.)
# reasoning-bank.jsonl is raw captured trajectories (runtime); docs/memory/README.md is tracked.
docs/memory/reasoning-bank.jsonl
# standards-mine writes generated de-facto conventions here on --write; regenerated, not source.
docs/standards/
docs/audits/*-events.jsonl
docs/audits/*-verdicts.jsonl
docs/audits/gate-trips.jsonl
# judge position-sensitivity telemetry (debate-engine debiasedVerdict) — a validator-regenerated
# stream, NOT the mistake ledger. Kept out of docs/audits/meta-mistakes.jsonl on purpose (ledger-pollution, 2026-07-04).
docs/audits/judge-debias-telemetry.jsonl

# OS / editor
.DS_Store
*.swp

# Logs
*.log
npm-debug.log*

# Dependencies (if a host project adds them)
node_modules/
.claude/settings.local.json

# Generated by post-commit surface-concerns hook
docs/surfacing-concerns-current.md
docs/audit-reports/

# jidoka: cron-audit runtime log (not committed)
docs/audits/cron-runs.jsonl
docs/audits/approval-queue.jsonl
docs/audits/decision-log.jsonl
docs/audits/chaos-test-stream.jsonl
docs/audits/tui-panel-launches.jsonl

# Session / runtime artifacts (regenerated, not source)
.playwright-mcp/
dashboard-current.png
docs/CURRENT_WAVE.md
docs/dashboard-snapshot.md
docs/audits/spec-context-runs.jsonl
docs/audits/clarify-runs.jsonl
docs/audits/lesson-utility.json

# local install/test artifacts — engine tests are zero-dep by design; products own their lockfiles
package-lock.json
test-results/
# Kaizen engine: _DASHBOARD.md is a regenerable render (local only). The two registries
# below are the accountability backbone and ARE committed with each weekly report — the
# design doc (KAIZEN_ENGINE.md) and the weekly task require it, so they are NOT ignored.
docs/research/weekly/_DASHBOARD.md
